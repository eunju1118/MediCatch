// Legacy Sidebar — now aliased to the new top Navbar so any stale import
// (bundler cache, HMR ghost, etc.) still renders the new MediCatch layout.
import Navbar from './Navbar';
export default Navbar;
